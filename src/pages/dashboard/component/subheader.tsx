import { Typography, Box } from "@mui/material";

const Subheader = () => {
  return (
    <Box>
      <Typography variant="h6">Formula Top Headlines</Typography>
    </Box>
  );
};

export { Subheader };
